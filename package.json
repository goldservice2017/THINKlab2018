{
  "name": "think2018-cnc-lab",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browsersync": "browser-sync start --reload-delay:5000 --proxy 'localhost:3000' --port 3001 --files 'client/**/*.*'",
    "watch": "nodemon --watch .",
    "develop": "concurrently \"npm run watch\" \"sleep 5 && npm run browsersync\"; exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^5.0.0",
    "express": "^4.16.2",
    "multer": "^1.3.0",
    "nodemon": "^1.15.0",
    "path": "^0.12.7",
    "request": "^2.83.0"
  },
  "devDependencies": {
    "browser-sync": "^2.23.6",
    "concurrently": "^3.5.1"
  }
}
